// {{name}} Tests
// Auto-generated by CodePilot from project patterns

{{imports}}

describe('{{Name}}', () => {
  {{beforeEach}}

  it('should {{happyPath}}', {{asyncKeyword}} () => {
    // Arrange
    {{arrange}}

    // Act
    {{act}}

    // Assert
    {{assert}}
  })

  it('should handle {{edgeCase}}', {{asyncKeyword}} () => {
    {{edgeCaseTest}}
  })

  it('should throw when {{errorCase}}', {{asyncKeyword}} () => {
    {{errorCaseTest}}
  })
})
